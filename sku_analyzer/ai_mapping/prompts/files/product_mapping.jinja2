**PRODUCT MAPPING TASK**

**Parent SKU:** {{ parent_sku }}
**Business Context:** {{ business_context or "German Amazon marketplace product" }}

**MANDATORY FIELDS TO MAP:**
{% for field_name, field_info in mandatory_fields.items() %}
- **{{ field_name }}** ({{ field_info.display_name }})
  - Type: {{ field_info.data_type }}
  - Valid values: {{ field_info.valid_values[:5] | join(", ") }}{% if field_info.valid_values|length > 5 %} (and {{ field_info.valid_values|length - 5 }} more){% endif %}
  - Constraints: {{ field_info.constraints }}
{% endfor %}

**SOURCE PRODUCT DATA:**
```json
{{ product_data | tojson(indent=2) }}
```

**MAPPING INSTRUCTIONS:**
1. Analyze each source data field and determine the best mapping to mandatory fields
2. Consider field names, values, and context for intelligent mapping
3. Only create mappings with >70% confidence
4. Preserve data integrity and respect constraints
5. Handle German language context appropriately

**REQUIRED OUTPUT FORMAT:**
```json
{
  "parent_sku": "{{ parent_sku }}",
  "mapped_fields": [
    {
      "source_field": "SOURCE_FIELD_NAME",
      "target_field": "MANDATORY_FIELD_NAME", 
      "mapped_value": "TRANSFORMED_VALUE",
      "confidence": 0.95,
      "reasoning": "Clear explanation of mapping logic"
    }
  ],
  "unmapped_mandatory": ["list", "of", "unmapped", "fields"],
  "overall_confidence": 0.87,
  "processing_notes": "Summary of mapping process and key decisions"
}
```

Map the product data now: